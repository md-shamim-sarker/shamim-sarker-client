import React from 'react';
import Navbar from '../components/Navbar';

const NotesLayout = () => {
    return (
        <div>
            <Navbar></Navbar>
        </div>
    );
};

export default NotesLayout;